.classic-container {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.classic-img {
  width: 750rpx;
  height: 500rpx;
}

.tag {
  width: 46rpx;
  height: 142rpx;
  position: relative;
  right: 310rpx;
  bottom: 58rpx;
}

.content {
  /* 自适应宽度, 当宽度比较小的时候也能居中，那么使用max-width */
  max-width: 550rpx;
  font-size: 36rpx;
}
